# ═══════════════════════════════════════════════════════════════════════════════
# DEVELOPMENT ENVIRONMENT CONFIGURATION
# ═══════════════════════════════════════════════════════════════════════════════

# Application Configuration
APP_NAME=RMS-Shopify Integration
APP_VERSION=0.1.0
ENVIRONMENT=development
DEBUG=True
HOST=0.0.0.0
PORT=8080
WORKERS=1
LOG_LEVEL=DEBUG

# RMS Database Configuration
RMS_DB_HOST=190.106.75.222
RMS_DB_PORT=1438
RMS_DB_NAME=bb57
RMS_DB_USER=Shop1fy5428
RMS_DB_PASSWORD=sh0pqfy10736!
RMS_DB_DRIVER=ODBC Driver 17 for SQL Server
RMS_CONNECTION_TIMEOUT=60

# Shopify Configuration
SHOPIFY_SHOP_URL=your-store.myshopify.com
SHOPIFY_ACCESS_TOKEN=your-shopify-access-token
SHOPIFY_API_VERSION=2025-04
SHOPIFY_WEBHOOK_SECRET=your-webhook-secret

# Redis Configuration
REDIS_URL=redis://localhost:6379/0

# Synchronization Settings
ENABLE_SCHEDULED_SYNC=True
SYNC_INTERVAL_MINUTES=5
SYNC_BATCH_SIZE=25
SYNC_CHECKPOINT_INTERVAL=100
SYNC_MAX_CONCURRENT_JOBS=3

# ═══════════════════════════════════════════════════════════════════════════════
# ORDER SYNCHRONIZATION SYSTEM (Shopify → RMS)
# ═══════════════════════════════════════════════════════════════════════════════

# === PRIMARY METHOD: Order Polling (RECOMMENDED) ===
ENABLE_ORDER_POLLING=True
ORDER_POLLING_INTERVAL_MINUTES=5
ORDER_POLLING_LOOKBACK_MINUTES=10
ORDER_POLLING_BATCH_SIZE=50
ORDER_POLLING_MAX_PAGES=10

# === OPTIONAL: Webhooks (DEVELOPMENT TESTING) ===
# Enable both methods in development for testing comparison
ENABLE_WEBHOOKS=True

# Order Filtering
ALLOWED_ORDER_FINANCIAL_STATUSES=PAID,PARTIALLY_PAID,AUTHORIZED,PENDING

# Guest Customer Configuration
ALLOW_ORDERS_WITHOUT_CUSTOMER=True
GUEST_CUSTOMER_ACCOUNT_NUMBER=SHOPIFY-GUEST
DEFAULT_CUSTOMER_ID_FOR_GUEST_ORDERS=9111
REQUIRE_CUSTOMER_EMAIL=False

# ═══════════════════════════════════════════════════════════════════════════════
# GraphQL MCP Server (Testing)
# ═══════════════════════════════════════════════════════════════════════════════

# GraphQL API Endpoint URL (required)
GRAPHQL_API_ENDPOINT=https://countries.trevorblades.com/graphql

# API Key for authentication (optional)
GRAPHQL_API_KEY=

# Cache time-to-live in milliseconds (optional, default: 3600000 - 1 hour)
# CACHE_TTL=3600000

# Enable mutations (optional, default: false)
# ENABLE_MUTATIONS=false

# Optional whitelist of queries/mutations
# WHITELISTED_QUERIES=countries,continent,languages
# WHITELISTED_MUTATIONS=createUser,updateProfile
